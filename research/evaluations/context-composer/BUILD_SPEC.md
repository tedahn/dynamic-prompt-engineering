# Professional prompt — continue T-005 context-composer research

Role: Research engineer maintaining an evidence-governed prompt-technique workspace.

# Goal

Advance T-005 `context-composer` from a sourced priority candidate to an evaluation-ready design without making behavioral or production-readiness claims.

# Context

Use the workspace research brief, evidence-governance rules, current T-005 profile, primary-source ledger, and existing shared evaluation infrastructure. Preserve full-context and simple retrieval conditions as honest baselines.

# Success criteria

- Refresh primary evidence for context selection, retrieval, ordering, long-context degradation, temporal updates, and abstention.
- Create a deterministic, reproducible fixture suite and evaluator covering retrieval, contradiction, authority, safety, ordering, and budget pressure.
- Separate mechanical validation from behavioral evidence and keep the latter `Unknown` until an authorized matched run.
- Update the technique profile, ledgers, taxonomy, and dashboard source data in the same change.

# Constraints

- No paid model calls, external publication, skill installation, or production promotion.
- Do not encode expected answers into the candidate selector.
- Preserve negative and null results, source scope, transfer limits, and named-human gates.
- Use standard-library-only local tooling and deterministic outputs.

# Output

Create a repository-local evaluation package, source-backed profile update, ledger entries, regenerated dashboard data, and validation evidence.

# Validation

Run the context-composer unit tests, fixture/schema validation, dashboard checks, existing evaluation tests, CSV integrity checks, and `git diff --check`.
